general {
  colors = true
  interval = 1
}

order += "wireless _first_"
order += "ethernet _first_"
order += "tztime local"

wireless _first_ {
  format_up = "WIL"
  format_down = "WIL"
}

ethernet _first_ {
  format_up = "ETH"
  format_down = "ETH"
}

tztime local {
  format = "%d %m %Y / %H:%M"
}